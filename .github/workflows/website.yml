name: Website

on:
  push:
    branches: [main]
    paths:
      - "website/**"
      - "docs/PRE_RELEASE_MASTERPLAN.md"
      - "docs/USER_GUIDE_DE.md"
      - "docs/USER_GUIDE_EN.md"
      - "docs/PRIVACY_AND_DATA_INVENTORY.md"
      - "tool/validate_website.mjs"
      - ".github/workflows/website.yml"
  pull_request:
    paths:
      - "website/**"
      - "docs/PRE_RELEASE_MASTERPLAN.md"
      - "docs/USER_GUIDE_DE.md"
      - "docs/USER_GUIDE_EN.md"
      - "docs/PRIVACY_AND_DATA_INVENTORY.md"
      - "tool/validate_website.mjs"
      - ".github/workflows/website.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "irl-dolphin-web-pages"
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Validate website structure and content
        run: node tool/validate_website.mjs

      - name: Parse website JavaScript
        run: node --check website/app.js

  deploy:
    if: github.event_name != 'pull_request'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload website
        uses: actions/upload-pages-artifact@v4
        with:
          path: website

      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@v4
